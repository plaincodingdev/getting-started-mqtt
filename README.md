# Run the Project

Start the broker service locally:

```bash
docker compose up -d
```

Install dependencies and run the publisher and subscriber:

```bash
npm install
npm run publisher
npm run subscriber
```
